#pragma once

namespace selaura {
	struct symbol {
		// represent symbol with enum
		// for example:
		// (variable) GuiData_mGuiScale -> float
		// (function) ScreenView_SetupAndRender -> void(__thiscall*)(ScreenView* _thisptr, MinecraftUIRenderContext* ctx);
	};
};